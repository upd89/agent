class System:

    def __init__(self, name, urn, os, address, tag, certificate):
        self.name = name
        self.urn = urn
        self.os = os
        self.address = address
        self.tag = tag
        self.certificate = certificate
